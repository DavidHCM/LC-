#include <iostream>

using namespace std;

int main() {
    /*
    string name = "gri Academy";
    // Funcion de .find busca un parametro donde queremos un caracter o string
    cout << name.find("Academy",0) << endl;
    // Aqui usando este ejemplo me regresa el index donde lo encontro

    // Aqui tomando dos parametros nos dara elementos de un cierto string y tambien podemos pedir la longitud a tomar
    cout << name.substr(4,3) << endl; // Nos dara: Aca
     */

    /*
    string name;
    cout << "name: ";
    getline(cin,name); // Para tomar toda la linea del user, donde mandamos como parametro el cin que entrara y la variable a almacenar
    // De solo necesitar un elemento de char entonces solo usar el cin
    cout << name;
     */

    /*
    bool sent = true;
    int a,b;
    while(sent)
    {

            int opc;
            cout << "Que operacion quieres hacer: " << endl;
            cout << "1-Sumar: 2-Restar: 3-Multiplicar: 4-Salir: " << endl;
            cin >> opc;
            switch (opc)
            {
                case 1:
                    cout << "Dime el primer numero a sumar: ";
                    cin >> a;
                    cout << "Dime el segundo numero a sumar: ";
                    cin >> b;
                    cout << "Resultado: " << (a + b) << endl;
                    a = 0;
                    b = 0;
                    break;
                case 2:
                    cout << "Dime el primer numero a restar: ";
                    cin >> a;
                    cout << "Dime el segundo numero a restar: ";
                    cin >> b;
                    cout << "Resultado: " << (a - b) << endl;
                    a = 0;
                    b = 0;
                    break;
                case 3:
                    cout << "Dime el primer numero a multiplicar: ";
                    cin >> a;
                    cout << "Dime el segundo numero a multiplicar: ";
                    cin >> b;
                    cout << "Resultado: " << (a * b) << endl;
                    a = 0;
                    b = 0;
                    break;
                case 4:
                    sent = false;
            }

    }
     */

    // Story creations to practice strings

    return 0;
}
